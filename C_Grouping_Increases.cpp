#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(false); cin.tie (NULL)
#define endl '\n'
using namespace std;

void solve() {
    fastio;
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) cin >> v[i];
    int l = 0, r = 0;
    if()
}

int main() {
    int tt;
    cin >> tt;
    while(tt--) {
        solve();
    }
}